#include "pollard_att.h"

void pollard_att::_pollards(mpf_t n){
  	/*TODO:If either p-1 or q-1 have only small prime factors,
   	  then n can be factored using Pollard's p-1 alg
  	*/

   	mpf_t k, p, q, temp;
   	mpf_init(k);
  	mpf_init(p);
  	mpf_init(q);
  	mpf_init(temp);


}